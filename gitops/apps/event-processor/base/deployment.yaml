apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-processor
  namespace: matchsense
  labels:
    app: event-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-processor
  template:
    metadata:
      labels:
        app: event-processor
    spec:
      enableServiceLinks: false
      containers:
        - name: event-processor
          image: ghcr.io/nhatminh06/event-processor@sha256:e7c266a785fc016ebd09ee6f4462256014f18e693182f5193e68ada45c0bc2e4
          ports:
            - containerPort: 8081
          env:
            - name: KAFKA_BROKER
              value: "kafka:29092"
            - name: REDIS_URL
              value: "redis:6379"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "jaeger:4318"
            - name: METRICS_PORT
              value: "8081"
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 3
            periodSeconds: 5
