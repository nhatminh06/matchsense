apiVersion: apps/v1
kind: Deployment
metadata:
  name: query-api
  namespace: matchsense
  labels:
    app: query-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: query-api
  template:
    metadata:
      labels:
        app: query-api
    spec:
      enableServiceLinks: false
      containers:
        - name: query-api
          image: ghcr.io/nhatminh06/query-api@sha256:e1d168d40a41a43d032736e7e9c48b07ecfc0dab808d97c9bdfef5800b3575d7
          ports:
            - containerPort: 8083
          env:
            - name: PORT
              value: "8083"
            - name: REDIS_URL
              value: "redis:6379"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "jaeger:4318"
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 3
            periodSeconds: 5
