apiVersion: v1
kind: Service
metadata:
  name: event-api
  namespace: matchsense
spec:
  type: NodePort
  selector:
    app: event-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080